@import "../../public/css/variables.pcss";

.button {
  background-color: var(--active);
  padding: 8px 15px;
  color: #fff;
  border-radius: 1px;
  border: 0;
  font-weight: bold;
  cursor: pointer;
  font-size: 16px;

  &:hover {
    opacity: 0.9;
  }

}