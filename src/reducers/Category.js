import {

} from '../constants/Category'

const initialState = {
  types: ['Библиотеки', 'Школы', 'Детские сады'],
  items: [

  ]
}

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
